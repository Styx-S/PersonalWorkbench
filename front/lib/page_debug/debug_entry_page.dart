
import 'package:flutter/material.dart';
import 'package:workbench/Foundations/foundations.dart';

class DebugEntryPage extends StatelessWidget {
  const DebugEntryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return TitlePage(
        title: "调试入口",
        pageBody: Container(),
      );
  }

}