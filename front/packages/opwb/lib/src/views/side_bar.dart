
import 'package:flutter/widgets.dart';

class SideBarItem {

}

class SideBar extends StatelessWidget {
  final String? title;
  const SideBar(this.title, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }

}